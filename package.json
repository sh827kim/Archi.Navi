{
  "name": "archi-navi",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first architecture navigation tool for distributed service environments",
  "scripts": {
    "dev": "turbo run dev --filter=@archi-navi/web",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:coverage": "pnpm --filter @archi-navi/shared test:unit:coverage && pnpm --filter @archi-navi/db test:unit:coverage && pnpm --filter @archi-navi/cli test:unit:coverage && pnpm --filter @archi-navi/ui test:unit:coverage && pnpm --filter @archi-navi/core test:unit:coverage && pnpm --filter @archi-navi/inference test:unit:coverage",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\" --ignore-path .gitignore",
    "clean": "turbo run clean && rm -rf node_modules",
    "db:generate": "turbo run db:generate --filter=@archi-navi/db",
    "db:migrate": "turbo run db:migrate --filter=@archi-navi/db",
    "db:studio": "turbo run db:studio --filter=@archi-navi/db",
    "release:pack:npm": "bash ./scripts/release/pack-npm.sh",
    "release:publish:npm:dry-run": "bash ./scripts/release/publish-npm.sh --dry-run",
    "release:publish:npm": "bash ./scripts/release/publish-npm.sh"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "prettier": "^3.8.1",
    "tailwindcss": "^4.2.1",
    "turbo": "^2.8.12",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.26.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ],
    "overrides": {
      "drizzle-orm": "^0.45.1"
    }
  }
}
